# src/ranges_dashboard/apps.py

from django.apps import AppConfig

class RangesDashboardConfig(AppConfig):
    name = 'ranges_dashboard'
    label = 'ranges_dashboard'
    # verbose_name можно оставить пустым или задать для ясности